server:
  port: 8080

spring:
  application:
    name: user
  datasource:
    username: 'mourad'
    url: jdbc:postgresql://localhost:5432/user
    password: 'password'

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true